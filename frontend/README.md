# 前端界面

现代响应式服务器监控前端界面，基于原生 HTML5 技术栈。

## 🎨 项目结构

```
frontend/
├── assets/               # 静态资源
│   ├── fonts/           # 字体文件
│   ├── icons/           # 图标资源
│   └── images/          # 图片资源
├── components/          # 可复用组件
│   ├── header/         # 头部导航组件
│   ├── server-card/     # 服务器卡片组件
│   ├── chart-panel/     # 图表面板组件
│   └── status-panel/   # 状态面板组件
├── css/                # 样式文件
│   ├── base.css        # 基础样式
│   ├── components.css  # 组件样式
│   ├── layout.css      # 布局样式
│   └── themes/         # 主题样式
│       ├── dark.css    # 暗色主题
│       └── light.css   # 亮色主题
├── js/                 # JavaScript 逻辑
│   ├── app/           # 应用核心逻辑
│   │   ├── app.js     # 主应用
│   │   ├── config.js  # 配置管理
│   │   └── utils.js   # 工具函数
│   ├── charts/        # 图表相关
│   │   ├── charts.js  # 图表管理器
│   │   ├── cpu-chart.js # CPU 图表
│   │   └── memory-chart.js # 内存图表
│   ├── components/    # 组件逻辑
│   │   ├── header.js  # 头部逻辑
│   │   └── server-card.js # 卡片逻辑
│   └── services/     # 服务层
│       ├── api.js    # API 服务
│       └── storage.js # 本地存储
├── config/            # 配置文件
│   ├── servers.json   # 服务器配置
│   └── settings.json # 应用设置
├── index.html        # 主页面
└── README.md        # 本文件
```

## 🚀 快速开始

### 直接使用
```bash
# 在浏览器中直接打开
open frontend/index.html

# 或使用 HTTP 服务器
cd frontend
python -m http.server 8000
# 访问 http://localhost:8000
```

### 开发模式
如需使用构建工具（可选）：

```bash
# 安装依赖（如果添加了构建工具）
npm install

# 启动开发服务器
npm run dev
```

## 🎯 功能特性

### 核心功能
- ✅ 实时数据监控
- ✅ 多服务器切换
- ✅ 响应式设计
- ✅ 暗色主题
- ✅ 数据可视化
- ✅ 键盘快捷键

### 技术特性
- **无框架依赖**：纯原生 HTML5/CSS3/JavaScript
- **模块化设计**：清晰的代码组织结构
- **性能优化**：优化的数据更新机制
- **浏览器兼容**：支持现代浏览器

## 🎨 界面设计

### 设计理念
采用现代科技监控仪表板风格，深色主题减少视觉疲劳，突出数据展示。

### 布局结构

```
┌─────────────────────────────────────────┐
│               头部导航                    │
├─────────────────────────────────────────┤
│  服务器卡片区    │       图表展示区        │
│                │                       │
│                │                       │
│                │                       │
│                │                       │
│                │                       │
│                │                       │
│                │                       │
└─────────────────────────────────────────┘
```

### 响应式断点
- **桌面端** (≥1200px)：三栏布局
- **平板端** (768px-1199px)：双栏布局  
- **移动端** (<768px)：单列布局

## ⚙️ 配置说明

### 服务器配置 (`config/servers.json`)

```json
{
  "servers": [
    {
      "id": "local",
      "name": "本地服务器",
      "url": "http://localhost:48877/api/status",
      "description": "本地开发环境",
      "type": "development"
    },
    {
      "id": "production-1",
      "name": "生产服务器 1", 
      "url": "http://192.168.1.100:48877/api/status",
      "description": "主要生产服务器",
      "type": "production"
    }
  ],
  "defaultServer": "local",
  "refreshInterval": 2000
}
```

### 应用设置 (`config/settings.json`)

```json
{
  "theme": "dark",
  "language": "zh-CN",
  "autoRefresh": true,
  "chartAnimation": true,
  "dataPoints": 60
}
```

## 🔧 自定义开发

### 添加新的监控指标

1. **后端支持**
   - 在后端添加新的 API 端点
   - 确保数据格式一致

2. **前端集成**
   ```javascript
   // 在 js/services/api.js 中添加数据获取方法
   async function fetchNewMetric() {
     const response = await fetch('/api/new-metric');
     return await response.json();
   }
   
   // 在 js/charts/ 中添加对应的图表组件
   class NewMetricChart extends BaseChart {
     // 图表实现逻辑
   }
   ```

3. **界面更新**
   - 在 HTML 中添加显示元素
   - 在 CSS 中定义样式
   - 在 JavaScript 中绑定交互逻辑

### 主题定制

修改 CSS 变量实现主题切换：

```css
:root {
  /* 深色主题 */
  --bg-primary: #1a1f36;
  --text-primary: #ffffff;
  --accent-color: #00d4ff;
}

[data-theme="light"] {
  /* 浅色主题 */
  --bg-primary: #ffffff;
  --text-primary: #333333;
  --accent-color: #007acc;
}
```

## 📱 移动端优化

### 触摸交互
- 支持手势操作
- 优化按钮大小（≥44px）
- 避免 hover 状态冲突

### 性能考虑
- 减少不必要的重绘
- 优化图片资源
- 使用 CSS 硬件加速

## 🚀 性能优化

### 数据更新优化
```javascript
// 防抖处理，避免频繁更新
function debounce(func, wait) {
  let timeout;
  return function executedFunction(...args) {
    const later = () => {
      clearTimeout(timeout);
      func(...args);
    };
    clearTimeout(timeout);
    timeout = setTimeout(later, wait);
  };
}
```

### 图表性能
- 限制数据点数量（默认60个）
- 使用 requestAnimationFrame
- 避免不必要的图表重绘

## 🔒 安全考虑

### 内容安全策略
在 HTML 中添加 CSP：

```html
<meta http-equiv="Content-Security-Policy" 
      content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net;">
```

### 输入验证
对所有用户输入进行验证：

```javascript
function validateServerUrl(url) {
  const pattern = /^https?:\/\/[\w.-]+(:\d+)?\/api\/status$/;
  return pattern.test(url);
}
```

## 🧪 测试

### 功能测试
```javascript
// 示例测试用例
describe('API Service', () => {
  it('should fetch server status correctly', async () => {
    const status = await fetchServerStatus();
    expect(status).toHaveProperty('cpu');
    expect(status).toHaveProperty('memory');
  });
});
```

### 浏览器兼容性测试
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 🐳 部署

### 静态文件部署
直接将 `frontend/` 目录部署到 Web 服务器：

```nginx
server {
    listen 80;
    server_name monitor.example.com;
    root /var/www/server-monitor/frontend;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

### CDN 部署
将静态资源上传到 CDN：

```bash
# 压缩和优化资源
npm run build  # 如果使用构建工具

# 上传到 CDN
aws s3 sync ./dist s3://your-bucket/frontend/
```

## 🚨 故障排除

### 常见问题

1. **图表不显示**
   - 检查 Chart.js 是否正确加载
   - 验证 API 数据格式
   - 查看浏览器控制台错误

2. **数据不更新**
   - 检查后端服务是否正常运行
   - 验证网络连接
   - 检查浏览器缓存

3. **布局错乱**
   - 检查 CSS 文件是否正确加载
   - 验证响应式断点
   - 清除浏览器缓存

### 调试技巧

```javascript
// 启用调试模式
localStorage.setItem('debug', 'true');

// 查看详细日志
console.log('API Response:', data);
console.log('Chart Config:', chartConfig);
```

## 📞 支持

- 界面问题：[GitHub Issues](链接)
- 功能建议：[GitHub Discussions](链接)
- 设计反馈：[设计文档](链接)

---

**前端界面** · 现代设计 · 响应式体验